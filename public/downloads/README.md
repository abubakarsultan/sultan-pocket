Place your generated Android APK file here, named exactly:

    sultan-pocket.apk

This is what the homepage "Download Android APK" button links to
(/downloads/sultan-pocket.apk). Get this file from PWABuilder
(pwabuilder.com) -> Package For Stores -> Android -> download the
package -> the .apk file inside is the one to put here (not the .aab,
that one goes to Play Store instead).
